# ZXCode 命令系统验收清单

> 机器可验证条目已按自动化测试勾选（全量 330 个测试通过）；最后一条"按 README 启动应用"需人工在真实终端验证。

## 注册中心与解析器

- [x] `python -m unittest tests.test_commands_registry tests.test_commands_parser -v` 全部通过。
- [x] 重复注册同名、同别名或大小写变体时抛异常（单测断言覆盖 ≥3 种冲突）。
- [x] 注册别名后，别名与主名称查询返回同一命令。
- [x] `/HELP x` 解析为命令名 `help`、参数 `x`；`/Plan` 解析为 `plan`。
- [x] 无 `/` 前缀的输入被判定为非命令。
- [x] 仅 `/` 或 `/  ` 输入被判定为未知命令且不调用模型。

## 分类与分发

- [x] 纯本地、UI 状态、AI 对话流三类命令各有一个单测用例验证走对应执行路径。
- [x] 命令处理函数抛异常时应用不崩溃，界面出现错误提示（单测断言无 traceback）。
- [x] 执行 AI 类命令后：JSONL 存档中包含触发消息（grep 触发文本 ≥1 条）。
- [x] 执行 AI 类命令后：JSONL 存档中不包含预设提示词全文（grep 预设关键词 = 0 条）。
- [x] AI 类命令的当次请求包含预设提示词（FakeClient 收到的请求中可查见）。
- [x] AI 类命令不计入自动笔记的强信号误判（无"我是…"等身份的预设词不触发笔记更新）。

## 内置命令

- [x] `/help` 输出包含全部可见命令、用法与参数提示（含 `/status`、`/permissions`、`/review`）。
- [x] `/status` 输出包含：模型名、轮次数、当前模式、token 估算、会话 ID 或"无会话"。
- [x] `/permissions` 输出安全策略摘要（含当前策略模式）。
- [x] `/review` 输出"待接入"占位说明，不调用模型。
- [x] `/sessions`、`/notes`、`/resume`、`/compact`、`/plan`、`/clear`、`/model`、`/exit` 迁移后行为不变。
- [x] `python -m unittest discover -s tests` 全部通过（用例数 ≥297）。

## Tab 补全

- [x] 输入 `/res` 按 Tab 单匹配补全为 `/resume`（无已输入参数时）。
- [x] 输入 `/s` 按 Tab 弹出多匹配选择列表，选中后输入框填入所选命令。
- [x] 隐藏命令不出现在补全列表，但直接输入可执行（单测断言）。
- [x] 无匹配前缀按 Tab 不改变输入框内容。
- [x] 已有参数时按 Tab 只补全命令名、保留参数（如 `/resume abc` 补全后不变）。

## 状态栏与引导

- [x] 状态栏常驻显示当前模式（执行/计划）与至少 2 个高频命令提示。
- [x] 执行 `/plan` 后状态栏模式从"执行"变为"计划"；再次执行切回。
- [x] 未知命令（如 `/nope`）提示包含 `/help` 引导，且 FakeClient 未被调用。

## 端到端

- [ ] 按项目 README 的启动方式运行应用，依次执行：`/HELP`、`/status`、`/plan`、Tab 补全（`/s`）、`/sessions`、`/notes`、`/clear`，每一步都有预期输出且无 traceback（需人工验证）。
- [x] 端到端执行一次 AI 类命令后，存档目录中对应会话的 jsonl 含触发消息、不含预设词（自动化断言）。
